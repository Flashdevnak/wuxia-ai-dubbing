name: Apply Transcript Button

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-transcript-button.yml'

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    timeout-minutes: 21
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      WORKER_URL: https://wuxia-ai-dubbing.26nak-testdev.workers.dev
    steps:
      - uses: actions/checkout@v6

      - name: Apply source patch
        run: python scripts/apply_transcript_button_patch.py

      - name: Validate source
        run: |
          node --check src/worker.js
          node --check public/app.js
          python -m py_compile scripts/extract_transcript_job.py scripts/youtube_transcript.py scripts/worker_client.py

      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/worker.js public/app.js public/index.html
          git commit -m "Add one-tap YouTube transcript extraction"
          git push origin main

      - uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Deploy upgraded Worker
        run: |
          npm install
          npx wrangler deploy

      - name: Verify deployed service
        run: |
          python - <<'PY'
          import json, os, urllib.request
          req = urllib.request.Request(os.environ['WORKER_URL'] + '/api/health', headers={'User-Agent':'Mozilla/5.0'})
          with urllib.request.urlopen(req, timeout=120) as r:
              health = json.load(r)
          assert health.get('driveReady') is True, health
          print('Transcript button Worker deployed: OK')
          PY
